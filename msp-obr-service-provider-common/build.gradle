
apply plugin: 'java'

bootJar {
    enabled = false
}

jar {
    enabled = true
}

dependencies {
    compile project (':msp-obr-common')
    compile "org.springframework.boot:spring-boot-starter-data-mongodb:${springBootVersion}"
}